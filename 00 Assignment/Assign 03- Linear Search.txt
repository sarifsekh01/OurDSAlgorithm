problem statement:03
write a programe to perfrom linear search on an array by accepting elements and a search key from the user and display wheater the element is form along with its position.

code:

#include <stdio.h>

int main() {
    int arr[100], n, key, i, found = 0;

    printf("Enter the number of elements: ");
    scanf("%d", &n);

    printf("Enter the elements: ");
    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    printf("Enter the element to search: ");
    scanf("%d", &key);

    for(i = 0; i < n; i++) {
        if(arr[i] == key) {
            found = 1;
            break;      // Exit the loop when the element is found
        }
    }

    if(found == 1)
        printf("Element found at position %d", i + 1);
    else
        printf("Element not found");

    return 0;
}